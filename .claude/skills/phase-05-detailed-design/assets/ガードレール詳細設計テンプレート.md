---
version: "1.0.0"
last_updated: "2026-05-09"
updated_by: ""
---

# ガードレール詳細設計書

> **参照元（システム設計資料）:**
> - ガードレール処理方式設計.md（ガードレール層の構成・実現方式・エージェントごとの適用方式・自律度対応）

> **参照元（要件定義資料）:**
> - 業務ルール定義.md（ブロック条件の具体的なルール値）

> **参照元（詳細設計資料）:**
> - エージェント詳細設計書.md（各エージェントの利用ツール・フック登録）

## 1. 概要

### 1.1 コンポーネントの目的

### 1.2 ガードレール種別と対象エージェント

| 種別 | 対象エージェント | 説明 |
|------|--------------|------|
| 入力フィルター | [エージェント] | [説明] |
| 出力フィルター | [エージェント] | [説明] |
| ツール実行ガード | [エージェント] | [説明] |

---

## 2. ガードレール一覧

| ガードレールID | 種別 | 対象エージェント | 概要 | 優先順位 |
|-------------|------|--------------|------|---------|
| GR-01 | [種別] | [エージェント] | [概要] | [優先順位] |
| GR-02 | [種別] | [エージェント] | [概要] | [優先順位] |

---

## 3. 入力フィルター設計

### 3.1 GR-[番号]: [ガードレール名]

#### 基本情報

| 項目 | 値 |
|------|-----|
| ガードレールID | GR-[番号] |
| 適用タイミング | ユーザー入力受信時 |
| 実装方式 | [方式（フックイベント名 or 直接実装）] |

#### ブロック条件

| 条件ID | 条件内容 | 判定ロジック |
|--------|---------|------------|
| [条件1] | [内容] | [ロジック（正規表現・キーワードマッチ等）] |
| [条件2] | [内容] | [ロジック] |

#### フォールバック応答

| 発動条件 | メッセージ文言 |
|---------|------------|
| [条件1] | "[メッセージ]" |
| [条件2] | "[メッセージ]" |

#### 処理フロー

```
ユーザー入力受信
  ↓
[入力テキスト取得]
  ↓
[ブロック条件判定]
  ↓
条件一致 → [フォールバック応答生成]
           ↓
           [ログ出力]
           ↓
           応答返却（処理中断）
条件不一致 → ガードレール通過
           ↓
           [通常処理継続]
```

---

## 4. 出力フィルター設計

### 4.1 GR-[番号]: [ガードレール名]

#### 基本情報

| 項目 | 値 |
|------|-----|
| ガードレールID | GR-[番号] |
| 適用タイミング | LLM応答生成後 |
| 実装方式 | [方式] |

#### フィルター条件

| 条件ID | 条件内容 | 判定ロジック | フィルター後処理 |
|--------|---------|------------|--------------|
| [条件1] | [内容] | [ロジック] | [処理（マスク・置換・削除等）] |

#### 処理フロー

```
LLM応答生成
  ↓
[応答テキスト取得]
  ↓
[フィルター条件判定]
  ↓
条件一致 → [応答修正・置換処理]
           ↓
           [ログ出力]
           ↓
           修正済み応答を返却
条件不一致 → ガードレール通過
           ↓
           [元の応答を返却]
```

---

## 5. ツール実行ガード設計

### 5.1 承認要否マトリクス

| エージェント | ツール名 | 承認要否 | 承認者 |
|-----------|--------|---------|--------|
| [エージェント] | [ツール名] | 要/不要 | [承認者] |

### 5.2 人間承認フックとの連携

| 項目 | 値 |
|------|-----|
| 実装方式 | ハンドラー詳細設計書記載のHumanApprovalHook |
| 承認待機タイムアウト | [秒] |
| タイムアウト時の動作 | [動作] |

---

## 6. 判定ロジック詳細

### 6.1 [ガードレールID] 判定ロジック

| 項目 | 値 |
|------|-----|
| 使用データソース | [ファイルパス/インメモリ定義] |
| データ形式 | [形式] |
| 判定アルゴリズム | [アルゴリズム名・説明] |
| 計算量 | [オーダー] |

---

## 7. エラーハンドリング

### 7.1 エラー処理フロー

```
ガードレール処理中エラー発生
  ↓
[エラー種別判定]
  ↓
[ErrorHandlerへ委譲]
  ↓
フェイルオープン（通過）/フェイルクローズ（ブロック）
```

### 7.2 エラーメッセージ

| エラー種別 | 発生条件 | 動作 | ユーザー向けメッセージ |
|----------|---------|------|---------------------|
| [エラー1] | [条件] | [動作] | "[メッセージ]" |

---

## 8. ログ出力

| レベル | タイミング | メッセージ |
|--------|-----------|-----------|
| INFO | ガードレール通過時 | "[メッセージ]" |
| WARNING | ガードレール発動時 | "[メッセージ]" |
| ERROR | 判定処理エラー時 | "[メッセージ]" |

---

## 9. 補足情報

### 9.1 実装上の注意点

1. **[注意点1]**
   - [詳細]

---

## 10. 依存関係

### 10.1 外部ライブラリ
- `strands.hooks`: Strandsフックイベント（該当する場合）
  - `[イベントクラス名]`: [説明]

### 10.2 内部モジュール
- `handlers.error_handler`: エラーハンドリング
  - `ErrorHandler`: エラーハンドラー
- `[その他モジュール]`: [説明]

---

## 11. テスト観点

### 11.1 正常系テスト（通過）
- [テスト項目1（通過するケース）]
- [テスト項目2（通過するケース）]

### 11.2 ガードレール発動テスト
- [テスト項目1（ブロックされるケース）]
- [テスト項目2（フィルターされるケース）]

### 11.3 境界値テスト
- [ブロック判定の閾値付近のテスト]

### 11.4 フォールバック応答テスト
- [フォールバックメッセージの内容確認テスト]

---

## 12. 変更履歴

| 日付 | バージョン | 変更内容 |
|------|-----------|---------|
| YYYY-MM-DD | 1.0 | 初版作成 |
